<mvc:View
    controllerName="mock.travels.controller.Dashboard"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m">

    <App>
        <Page title="Travel Dashboard" class="sapUiResponsiveContentPadding">

            <content>
                <SearchField
                    id="searchCustomer"
                    width="50%"
                    class="sapUiSmallMarginBottom"
                    placeholder="Search by customer name..."
                    search=".onSearch"/>

                <Table 
                    id="customersTable"
                    items="{
                        path: '/Customers',
                        parameters: {
                            $$groupId: '$direct'
                        }
                    }">

                    <columns>
                        <Column>
                            <Text text="Customer" />
                        </Column>
                        <Column>
                            <Text text="City" />
                        </Column>
                        <Column>
                            <Text text="Country" />
                        </Column>
                    </columns>

                    <items>
                        <ColumnListItem>
                            <cells>
                                <Text text="{CustomerName}" />
                                <Text text="{City}" />
                                <Text text="{Country}" />
                            </cells>
                        </ColumnListItem>
                    </items>
                </Table>
            </content>

        </Page>
    </App>

</mvc:View>